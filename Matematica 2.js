//Crear 4 variables
let num1 = 6;
let num2 = 4;
let num3 = 8;
let num4 = 5;

//Suma de las dos primeras variables\
let sum = num1 + num2;

//Restar la cuarta variable de la tercera
let difference = num4 - num3;

//Multiplicar resultados linea 8 y 11
let finalResult = sum * difference;

//Verificar si el total es un numero par
let even0ddResult = finalResult % 2 == 0;

console.log("El resultado final:", finalResult);
console.log("El resultado es par?", evenOddResult);

//Multiplicar ambos resultados
result = result + result2;

// Formatear el resultado
finalResult = result.toFixed(2);

//Verificar el dato del resultado final
console.log("Tipo de dato de resultado final antes de la conversacion:", typeof finalResult);

//Convertir 
let finalNumber = parseFloat(finalResult);

console.log("El valor del numero final es:", finalNumber);